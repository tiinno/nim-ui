{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "test:run": {
      "dependsOn": ["build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/.github/workflows/**",
        "$TURBO_ROOT$/scripts/**",
        "$TURBO_ROOT$/CLAUDE.md"
      ]
    }
  }
}
