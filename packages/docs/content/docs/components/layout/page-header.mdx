---
title: PageHeader
description: Backoffice page header with title, metadata, description, and action area
---

import {
  Button,
  PageHeader,
  PageHeaderActions,
  PageHeaderDescription,
  PageHeaderMeta,
  PageHeaderTitle,
} from '@/components/nim';

The PageHeader component gives admin and backoffice screens a consistent top structure for page title, operational context, and primary actions.

## Import

```tsx
import {
  PageHeader,
  PageHeaderActions,
  PageHeaderDescription,
  PageHeaderMeta,
  PageHeaderTitle,
} from '@nim-ui/components';
```

## Basic Usage

<ComponentPreview title="Backoffice Page Header" className="block">
  <PageHeader className="w-full">
    <div className="space-y-2">
      <PageHeaderMeta>Updated 2 minutes ago</PageHeaderMeta>
      <PageHeaderTitle>Orders</PageHeaderTitle>
      <PageHeaderDescription>
        Review exceptions, fulfillment status, and risk signals before the next warehouse handoff.
      </PageHeaderDescription>
    </div>
    <PageHeaderActions aria-label="Order actions">
      <Button variant="outline" size="sm">Export</Button>
      <Button variant="primary" size="sm">Create order</Button>
    </PageHeaderActions>
  </PageHeader>

  <PreviewCode>
```tsx
<PageHeader>
  <div className="space-y-2">
    <PageHeaderMeta>Updated 2 minutes ago</PageHeaderMeta>
    <PageHeaderTitle>Orders</PageHeaderTitle>
    <PageHeaderDescription>
      Review exceptions, fulfillment status, and risk signals before the next warehouse handoff.
    </PageHeaderDescription>
  </div>
  <PageHeaderActions aria-label="Order actions">
    <Button variant="outline" size="sm">Export</Button>
    <Button variant="primary" size="sm">Create order</Button>
  </PageHeaderActions>
</PageHeader>
```
  </PreviewCode>
</ComponentPreview>

## Props

### PageHeader

<PropsTable
  props={[
    {
      name: 'className',
      type: 'string',
      description: 'Additional classes for the header container.',
    },
    {
      name: 'children',
      type: 'ReactNode',
      required: true,
      description: 'Header content, usually title block and PageHeaderActions.',
    },
  ]}
/>

### PageHeaderActions

<PropsTable
  props={[
    {
      name: 'aria-label',
      type: 'string',
      description: 'Accessible label for the grouped actions.',
    },
    {
      name: 'children',
      type: 'ReactNode',
      required: true,
      description: 'Action buttons or controls for the page.',
    },
  ]}
/>

## Backoffice Guidance

- Keep one primary action in `PageHeaderActions`.
- Put freshness, ownership, or operational scope in `PageHeaderMeta`.
- Use the description for decision context, not marketing copy.
